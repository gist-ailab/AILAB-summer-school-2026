# 주의: 반드시 저장소 루트에서 실행할 것
# (requirements 파일의 로컬 상대경로는 파일 위치가 아닌 현재 작업 디렉토리 기준으로 해석됨)
#   pip install -r requirements.txt
#
# 사전 조건: Isaac Lab이 먼저 설치되어 있어야 함 (README의 Isaac Lab 섹션 참고)

opencv-python==4.11.0.86
# PyPI publishes Open3D 0.19 for x86_64, but ARM64 wheels currently stop at
# 0.18. The course uses APIs available in both releases.
open3d==0.19.0; platform_machine != "aarch64"
open3d==0.18.0; platform_machine == "aarch64"

# cgnet
easydict==1.13
scikit-learn==1.9.0
scipy==1.15.3
numpy==1.26.0
./cgnet/graspnetAPI

# SAM3
pycocotools==2.0.11
# editable 설치: 소스가 ./src/sam3 에 클론되어 직접 수정 가능
-e git+https://github.com/facebookresearch/sam3.git#egg=sam3
# 소스 수정이 필요 없다면 위 줄을 주석 처리하고 아래 줄을 사용
# sam3 @ git+https://github.com/facebookresearch/sam3.git

# 데이터 다운로드용 (setup.sh 에서 사용)
gdown==5.2.0

# IsaacLab
# 주의: isaacsim-kernel 은 typing_extensions==4.12.2 를 핀하지만, 최신 jupyter_client
# (ipykernel 의존성)가 typing_extensions>=4.13 의 TypedDict 문법을 사용한다. 4.16 에서
# Isaac 런타임은 정상 동작하므로(상위 호환) 노트북 실행을 위해 4.13+ 로 올린다.
# 설치 시 pip 이 핀 불일치 경고를 내지만 무시해도 된다.
typing_extensions>=4.13
stable_baselines3==2.3.2
starlette==0.49.1
psutil==5.9.8
uvicorn==0.29.0

# day1 노트북 실습용 (Jupyter 커널)
ipykernel==7.3.0
